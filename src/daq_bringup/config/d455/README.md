# D455 configuration

- `d455.yaml`: ROS 2 stream, topic, TF, resolution, and frame-rate settings.
- `*.json`: RealSense Viewer advanced-mode device settings.

The default launch combination is `d455.yaml` with
`d455_MD_emitter_off.json`.
